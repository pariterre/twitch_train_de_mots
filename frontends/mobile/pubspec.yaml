name: mobile
description: "Implementation of the mobile of the frontend"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  frontend_common:
    path: ../frontend_common
  common:

dependency_overrides:
  common:
    path: ../../common

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
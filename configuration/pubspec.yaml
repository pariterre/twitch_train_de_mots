name: configuration
description: "Implementation of the configuration package"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  common:
    path: ../common
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

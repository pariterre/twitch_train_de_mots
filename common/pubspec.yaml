name: common
description: Common things to share between the EBS server, the App and the Frontend
version: 1.0.0

environment:
  sdk: ^3.4.4

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  shared_preferences: ^2.2.0
  collection: ^1.18.0
  diacritic: ^0.1.4

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
